<Window x:Class="PizzaShop.UI.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PizzaShop.UI.Desktop"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DockPanel  >
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Datei"/>
            <MenuItem Header="Bearbeiten"/>
            <MenuItem Header="HILFE!!!"/>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom" Height="23" Background="Brown"/>

        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label>Test123</Label>
            <Button Grid.Column="1" AutomationProperties.AutomationId="b1" Click="Button_Click">Klick mich!</Button>
            <TextBox Grid.Column="2" AutomationProperties.AutomationId="tb1" Name="tb1" Text="---"></TextBox>

        </Grid>

    </DockPanel>

</Window>
